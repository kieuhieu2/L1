Sever:
    port: 8080
    servlet:
        context-path: /L1

spring:
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        url: "jdbc:mysql://localhost:3306/L1"
        username: root
        password: root
    jpa:
        hibernate:
            ddl-auto: update
            show-sql: true
